[package]
name = "vpram-engine"
version = "0.1.0"
edition = "2021"
authors = ["VPRAM Team"]
description = "Vector Parallel Random Access Memory Engine - Rust Integration Layer"
license = "MIT OR Apache-2.0"
repository = "https://github.com/anuceo/VPRAM-Engine"

[lib]
name = "vpram_engine"
path = "src/lib.rs"

[dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }

# gRPC server
tonic = "0.11"
prost = "0.12"

# WebSocket support
tokio-tungstenite = "0.21"
futures-util = "0.3"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# jlrs will be added when ready to integrate Julia runtime
# jlrs = { version = "0.19", features = ["multi-rt"] }

[build-dependencies]
tonic-build = "0.11"

[dev-dependencies]

[features]
default = []
# Feature flag for when jlrs integration is ready
julia-runtime = []

[[bin]]
name = "vpram-server"
path = "src/bin/vpram_server.rs"

[[example]]
name = "basic_kernel"
path = "examples/basic_kernel.rs"

[[example]]
name = "grpc_client"
path = "examples/grpc_client.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[profile.dev]
opt-level = 0
debug = true
